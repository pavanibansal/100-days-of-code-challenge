Q63: Merge two arrays.

/*
Sample Test Cases:
Input 1:
3
1 2 3
2
4 5
Output 1:
1 2 3 4 5

*/
#include <stdio.h>

int main() 
{
    int n1, n2, i, j;
    
    scanf("%d", &n1);
    int arr1[n1];
    
   
    for (i = 0; i < n1; i++) 
    {
        scanf("%d", &arr1[i]);
    }
    
    scanf("%d", &n2);
    int arr2[n2];
    
    // Input elements of second array
    for (i = 0; i < n2; i++) 
    {
        scanf("%d", &arr2[i]);
    }
    
    // Merge arrays
    int merged[n1 + n2];
    
    for (i = 0; i < n1; i++) 
    {
        merged[i] = arr1[i];
    }
    for (j = 0; j < n2; j++) 
    {
        merged[n1 + j] = arr2[j];
    }
    
  
    for (i = 0; i < n1 + n2; i++) 
    {
        printf("%d ", merged[i]);
    }
    
    return 0;
}
Q64: Find the digit that occurs the most times in an integer number.

/*
Sample Test Cases:
Input 1:
112233
Output 1:
1

Input 2:
887799
Output 2:
7

*/
#include <stdio.h>

int main() 
{
    int num;
    int count[10] = {0}, digit, i, max = 0, ans = 0;

    scanf("%d", &num);

    
    while (num > 0) 
    {
        digit = num % 10;     
        count[digit]++;       
        num = num / 10;       
    }

    // Find digit with highest count
    for (i = 0; i < 10; i++) 
    {
        if (count[i] > max) 
        {
            max = count[i];
            ans = i;
        }
    }


    printf("%d\n", ans);

    return 0;
}
